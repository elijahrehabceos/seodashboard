# Used by the Next.js app (safe to expose to the browser, read-only anon key)
NEXT_PUBLIC_SUPABASE_URL=https://gbozgrmfstbuqjbjhekk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Used only by the GitHub Action (never exposed to the browser)
# Set these as repo secrets instead of putting them in a committed .env file:
#   SE_RANKING_API_KEY
#   LOCAL_FALCON_API_KEY
#   SUPABASE_URL
#   SUPABASE_SERVICE_ROLE_KEY
